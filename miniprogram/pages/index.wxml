<import src="../templates/abort.wxml"></import>

<view class="entry">
  <view wx:if="{{ loaded }}" class="entry_content">
    <view class="entry_content_tip" catch:tap="onMenuTap" data-button="routine">今日任务</view>
  </view>
</view>

<template
  wx:if="{{ !loaded }}"
  is="abort"
  data="{{message: '加载中..', iconInvisible: true }}"
></template>
